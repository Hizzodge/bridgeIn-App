//it will have Header component and users

import { FC } from "react";
import Header from "../components/Header";

const Home: FC = () => {
  return (
    <>
      <Header placeholder="chose a user" />
    </>
  );
};

export default Home;
